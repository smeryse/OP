# Интеграция с LLM API

Модуль для генерации JSON отчётов из методичек через различные AI API.

## Поддерживаемые провайдеры

### 1. Google Gemini (рекомендуется)
- **Бесплатный tier**: 1500 запросов/день
- **Регистрация**: https://ai.google.dev/
- **API ключ**: Получите на https://makersuite.google.com/app/apikey
- **Установка**: `pip install google-generativeai`

### 2. Groq
- **Бесплатный tier**: 6000 токенов/мин, 14400 запросов/день
- **Регистрация**: https://console.groq.com/
- **Установка**: `pip install groq`
- **Особенность**: Очень быстрый ответ

### 3. OpenAI (опционально)
- **Платный**, но есть бесплатный кредит для новых пользователей
- **Установка**: `pip install openai`

## Хранение API ключей

API ключи хранятся в файле `.api_keys.json` в корне проекта (не коммитится в git).

### Через GUI
1. Откройте вкладку "🤖 Генерация JSON через AI"
2. Выберите провайдера (Gemini или Groq)
3. Введите API ключ в поле "API ключ"
4. Нажмите "Сохранить" — ключ будет сохранён в `.api_keys.json`
5. При следующем запуске ключ автоматически загрузится

### Через код
```python
from report_generator.config import set_api_key, get_api_key

# Сохранить ключ
set_api_key("gemini", "your-api-key-here")

# Получить ключ
api_key = get_api_key("gemini")
```

### Альтернатива: переменные окружения
Если вы предпочитаете использовать переменные окружения, они имеют приоритет над файлом:
- `GEMINI_API_KEY`
- `GROQ_API_KEY`
- `OPENAI_API_KEY`

## Использование

### Через GUI
1. Откройте вкладку "🤖 Генерация JSON через AI"
2. Выберите файл методички (PDF, TXT, MD)
3. Выберите провайдера (Gemini или Groq)
4. Если ключ не сохранён, введите и нажмите "Сохранить"
5. (Опционально) Укажите номер и тему лабы
6. Нажмите "🚀 Сгенерировать JSON"

### Через код
```python
from report_generator.llm.generator import generate_lab_json

result = generate_lab_json(
    lab_file_path="path/to/lab.pdf",
    output_json_path="lab3.json",
    lab_number=3,
    lab_theme="Работа с файлами",
    provider="gemini",  # или "groq"
    api_key=None,  # Если None, берётся из config
)
```

## Получение API ключей

### Gemini API
1. Перейдите на https://ai.google.dev/
2. Войдите в Google аккаунт
3. Нажмите "Get API key"
4. Создайте новый проект или выберите существующий
5. Скопируйте API ключ

### Groq API
1. Перейдите на https://console.groq.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел API Keys
4. Создайте новый ключ
5. Скопируйте API ключ

## Безопасность

⚠️ **Важно**: Файл `.api_keys.json` добавлен в `.gitignore` и не будет коммититься в git. Не делитесь своими API ключами публично!

Если вы случайно закоммитили файл с ключами:
1. Удалите файл из истории git
2. Создайте новые ключи в консоли провайдера
3. Обновите ключи в `.api_keys.json`
