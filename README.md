# Генератор отчётов по лабораторным работам

Проект для автоматизации написания отчётов: по методичке и скриншотам получается JSON от нейросети, затем скрипт собирает из него HTML-отчёт по ГОСТу.

## Как это работает

1
1. **Методичка → нейросеть** (В разработке)  
   Загружаешь файл методички (PDF/TXT/MD) прямо в программу, выбираешь AI провайдера (Gemini или Groq), и получаешь **JSON** автоматически! Больше не нужно копировать из браузера.

2. **JSON + картинки → отчёт**  
   Запускаешь программу с графическим интерфейсом, указываешь:
   - путь к **JSON** отчёта по лабе (или генерируешь через AI);
   - (по желанию) путь к **base_info.json** — данные об универе, студенте, преподавателе;
   - папку с **картинками** (1.png, 2.png, … в порядке появления в отчёте);
   - куда сохранить **HTML**.

3. **HTML → печать/сдача**  
   Открываешь полученный HTML в LibreOffice Writer, правишь поля под ГОСТ (лево 30мм, верх 20мм, право 15мм, низ 15мм), убираешь верхний колонтитул, сохраняешь в ODT/DOCX или печатаешь.

## Запуск

Из **корня проекта** (папка `Operating System`):

```bash
python run.py
```

Откроется окно «Генератор отчётов лабораторных работ» с двумя вкладками:
- **🤖 Генерация JSON через AI** — создание JSON из методички через нейросеть
- **📄 Генерация HTML отчёта** — создание финального отчёта из JSON

### Через командную строку (CLI)

```bash
python -m report_generator.cli.main --lab 3 -o lab3.html --images "3. data/lab3/images"
```

Параметры:
- `--lab <номер>` — номер лабораторной работы (ищет JSON в `3. data/lab<N>/lab<N>.json`)
- `--json <путь>` — путь к JSON файлу (альтернатива `--lab`)
- `-o, --output <путь>` — выходной HTML файл
- `--base-info <путь>` — путь к base_info.json
- `--images <путь>` — папка с изображениями
- `--width <px>` — максимальная ширина изображений (по умолчанию 500)

## Генерация JSON через AI 🤖

### Поддерживаемые провайдеры

1. **Google Gemini** (рекомендуется)
   - Бесплатно: 1500 запросов/день
   - Регистрация: https://ai.google.dev/
   - Получить API ключ: https://makersuite.google.com/app/apikey

2. **Groq**
   - Бесплатно: 6000 токенов/мин, 14400 запросов/день
   - Регистрация: https://console.groq.com/
   - Очень быстрый ответ

### Использование

1. Откройте вкладку "🤖 Генерация JSON через AI"
2. Выберите файл методички (PDF, TXT, MD)
3. Выберите провайдера (Gemini или Groq)
4. Введите API ключ и нажмите "Сохранить" — ключ сохранится в `.api_keys.json` (не коммитится в git)
   - При следующем запуске ключ автоматически загрузится
   - Можно также использовать переменные окружения (имеют приоритет)
5. (Опционально) Укажите номер и тему лабы
6. Нажмите "🚀 Сгенерировать JSON"

Подробнее: см. `report_generator/llm/README.md`

## Возможности

- ✅ **Генерация JSON через AI** — больше не нужно копировать из браузера!
- ✅ Автоматическая замена «(Рис. N)» → «как показано на Рисунке N» в тексте
- ✅ Встраивание изображений в HTML через base64 (сжатие до указанной ширины)
- ✅ Объединение данных лабы с базовой информацией (универ, студент, преподаватель)
- ✅ Обработка конфликтов имён при копировании изображений (автоматическое переименование)
- ✅ Автопоиск base_info.json рядом с JSON файлом
- ✅ Поддержка PDF, TXT, MD файлов для методичек

## Установка зависимостей

### Базовые зависимости
```bash
pip install pillow jinja2
```

### Для генерации JSON через AI
```bash
# Для Gemini (рекомендуется)
pip install google-generativeai

# Или для Groq
pip install groq

# Для парсинга PDF
pip install PyPDF2
# или альтернатива (лучше работает с таблицами)
pip install pdfplumber
```

Или установите всё сразу:
```bash
pip install -r requirements.txt
```

Работает с Python 3.8+.

## Структура проекта

| Папка / файл | Назначение |
|--------------|------------|
| `run.py` | Запуск GUI из корня проекта |
| `report_generator/` | Основной пакет |
| `report_generator/core/` | Логика генерации (report.py, image.py, renderer.py, text_processing.py) |
| `report_generator/gui/` | Графический интерфейс (app.py) |
| `report_generator/cli/` | Командная строка (main.py) |
| `report_generator/llm/` | Интеграция с AI API (client.py, generator.py, text_extractor.py) |
| `report_generator/config.py` | Конфигурация путей |
| `2. scripts/templates/` | HTML-шаблоны отчёта (Jinja2) |
| `2. scripts/lab_prompt.txt` | Промпт для нейросети по оформлению лабы |
| `3. data/` | Данные: `base_info.json`, папки lab1, lab2, … с JSON и картинками |
| `4. reports/` | Готовые отчёты (например ODT после экспорта из LibreOffice) |
